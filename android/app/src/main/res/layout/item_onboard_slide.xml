<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingStart="32dp"
    android:paddingEnd="32dp"
    android:background="@color/mozosubz_bg_dark">

    <!-- Slide Icon -->
    <ImageView
        android:id="@+id/slideIcon"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_marginBottom="40dp"
        android:contentDescription="Slide illustration"/>

    <!-- Slide Title -->
    <TextView
        android:id="@+id/slideTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/mozosubz_text_primary"
        android:textSize="26sp"
        android:textStyle="bold"
        android:gravity="center"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="-0.02"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="16dp"/>

    <!-- Slide Description -->
    <TextView
        android:id="@+id/slideDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/mozosubz_text_secondary"
        android:textSize="15sp"
        android:gravity="center"
        android:lineSpacingExtra="6dp"/>

</LinearLayout>
