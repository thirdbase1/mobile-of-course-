package com.mozosubz.app;

// Biometric authentication has been removed.
// This stub is kept so no references to the class cause compile errors.
public class BiometricGuard {
    public interface Callback {
        void onSuccess();
        void onCancel();
    }
}
